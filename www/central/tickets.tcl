set title "#contact-support.Tickets#"
set context [list [list index "#contact-support.Support#"] $title]

# Tickets ordered by priority, includes unassigned tickets if any.
# Each list of tickets have option to include closed tickets matching filter.